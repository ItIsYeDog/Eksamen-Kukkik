<!DOCTYPE html>
<html lang="no-NO">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Registrer - Kukkikk Ano</title>
    <link href="/css/styles.css" rel="stylesheet">
</head>
<body class="bg-gray-50">
    <%- include('../partials/header') %>
    
    <main class="container mx-auto px-4 py-8">
        <div class="max-w-3xl mx-auto">
            <h1 class="text-4xl font-bold text-gray-900 mb-8 text-center">Registrer deg</h1>
            
            <% if (typeof error !== 'undefined') { %>
                <div class="bg-red-50 border-l-4 border-red-500 p-4 mb-6">
                    <p class="text-red-700"><%= error %></p>
                </div>
            <% } %>

            <form action="/auth/register" method="POST" class="bg-white rounded-xl shadow-sm p-6 space-y-6">
                <div>
                    <label for="navn" class="block text-gray-700 mb-2">Navn</label>
                    <input 
                        type="text" 
                        id="navn" 
                        name="navn" 
                        required 
                        class="w-full px-4 py-2 border border-gray-200 rounded-lg focus:outline-none focus:ring-2 focus:ring-primary/50 focus:border-primary"
                        value="<%= typeof oldInput !== 'undefined' ? oldInput.navn : '' %>"
                    >
                </div>

                <div>
                    <label for="epost" class="block text-gray-700 mb-2">E-post</label>
                    <input 
                        type="email" 
                        id="epost" 
                        name="epost" 
                        required 
                        class="w-full px-4 py-2 border border-gray-200 rounded-lg focus:outline-none focus:ring-2 focus:ring-primary/50 focus:border-primary"
                        value="<%= typeof oldInput !== 'undefined' ? oldInput.epost : '' %>"
                    >
                </div>

                <div>
                    <label for="passord" class="block text-gray-700 mb-2">Passord</label>
                    <input 
                        type="password" 
                        id="passord" 
                        name="passord" 
                        required 
                        minlength="8"
                        class="w-full px-4 py-2 border border-gray-200 rounded-lg focus:outline-none focus:ring-2 focus:ring-primary/50 focus:border-primary"
                    >
                </div>

                <div>
                    <label for="kontaktsprak" class="block text-gray-700 mb-2">Kontaktspråk</label>
                    <select 
                        id="kontaktsprak" 
                        name="kontaktsprak" 
                        required 
                        class="w-full px-4 py-2 border border-gray-200 rounded-lg focus:outline-none focus:ring-2 focus:ring-primary/50 focus:border-primary focus:cursor-pointer"
                    >
                        <option value="">Velg språk</option>
                        <option value="nordsamisk">Nordsamisk</option>
                        <option value="lulesamisk">Lulesamisk</option>
                        <option value="sørsamisk">Sørsamisk</option>
                        <option value="umesamisk">Umesamisk</option>
                        <option value="pitesamisk">Pitesamisk</option>
                        <option value="enaresamisk">Enaresamisk</option>
                        <option value="skoltsamisk">Skoltsamisk</option>
                        <option value="akkalasamisk">Akkalasamisk</option>
                        <option value="kildinsamisk">Kildinsamisk</option>
                        <option value="tersamisk">Tersamisk</option>
                    </select>
                </div>

                <div>
                    <label for="telefonnummer" class="block text-gray-700 mb-2">
                        Telefonnummer (f.eks. +47 123 45 678)
                    </label>
                    <input 
                        type="tel" 
                        id="telefonnummer" 
                        name="telefonnummer" 
                        required 
                        pattern="^\+\d{2,3}[\s-]?\d{3}[\s-]?\d{2,3}[\s-]?\d{3}$"
                        placeholder="+47 123 45 678"
                        class="w-full px-4 py-2 border border-gray-200 rounded-lg focus:outline-none focus:ring-2 focus:ring-primary/50 focus:border-primary"
                        value="<%= typeof oldInput !== 'undefined' ? oldInput.telefonnummer : '' %>"
                    >
                    <p class="text-sm text-gray-500 mt-1">Inkluder landskode (f.eks. +47 for Norge)</p>
                </div>

                <button 
                    type="submit" 
                    class="w-full bg-indigo-600 text-white py-3 rounded-lg hover:bg-indigo-500 transform hover:scale-[1.02] transition-all duration-200 shadow-sm hover:shadow-md hover:cursor-pointer"
                >
                    Registrer
                </button>
            </form>
        </div>
    </main>

    <%- include('../partials/footer') %>
</body>
</html>