<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Logg inn - Kukkik Ano</title>
    <link href="/css/styles.css" rel="stylesheet">
</head>
<body class="bg-gray-50">
    <%- include('../partials/header') %>
    
    <main class="container mx-auto px-4 py-8">
        <div class="max-w-3xl mx-auto">
            <h1 class="text-4xl font-bold text-gray-900 mb-8 text-center">Logg inn</h1>
            
            <% if (typeof error !== 'undefined') { %>
                <div class="bg-red-50 border-l-4 border-red-500 p-4 mb-6">
                    <p class="text-red-700"><%= error %></p>
                </div>
            <% } %>

            <form action="/auth/login" method="POST" class="bg-white rounded-xl shadow-sm p-6 space-y-6">
                <div>
                    <label for="epost" class="block text-gray-700 mb-2">E-post</label>
                    <input 
                        type="email" 
                        id="epost" 
                        name="epost" 
                        required 
                        class="w-full px-4 py-2 border border-gray-200 rounded-lg focus:outline-none focus:ring-2 focus:ring-primary/50 focus:border-primary"
                        value="<%= typeof oldInput !== 'undefined' ? oldInput.epost : '' %>"
                    >
                </div>

                <div>
                    <label for="passord" class="block text-gray-700 mb-2">Passord</label>
                    <input 
                        type="password" 
                        id="passord" 
                        name="passord" 
                        required 
                        class="w-full px-4 py-2 border border-gray-200 rounded-lg focus:outline-none focus:ring-2 focus:ring-primary/50 focus:border-primary"
                    >
                </div>

                <button 
                    type="submit" 
                    class="w-full bg-blue-600 text-white py-3 rounded-lg hover:bg-blue-700 transform hover:scale-[1.02] transition-all duration-200 shadow-sm hover:shadow-md"
                >
                    Logg inn
                </button>

                <p class="text-center text-gray-600">
                    Har du ikke en konto? 
                    <a href="/auth/register" class="text-blue-600 hover:text-blue-700">Registrer deg her</a>
                </p>
            </form>
        </div>
    </main>

    <%- include('../partials/footer') %>
</body>
</html>